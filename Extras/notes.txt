when sending async stuff using runtime

chrome.runtime.onMessage.addListener(function(message, sender, sendResponse)

    urfunction().then(sendResponse)

    return true

    OR

    async function test(){
        ur code here
        sendResponse(ur result)
    }

    test()
    return true
    ============================

    function GetViewportScreenshot(){
    return new Promise(function(resolve,reject){

        setTimeout(function(){
            chrome.tabs.captureVisibleTab({format:"png"},function(viewport_screenshot){
                if(viewport_screenshot){resolve(viewport_screenshot)}
                
            })
        },50)

    })

}

this is how u delay a bit

==============
    transform: translate(-50%, -50%);
    use this to center something on a screen

=============

If u change either the width or height of an image, the image will resize
proportionally (keeping the aspect ratio)

when using a slider, user ParseInt(slider.value) or Parsefloat to fix the problem
where the img somtimes increases in size whereas it had to decrease


===============

when creating a new tab with a new html file, 
first create the tab
use chrome.tabs.onUpdated to ensure that its loaded
THEN send it data if needed

==================


use textArea.innerHTML = ur text, in order to show \n. (there might be securtiy concerns)